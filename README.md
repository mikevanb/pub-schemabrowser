Simple webserver serving the SchemaBrowser

Can run locally or in Azure

To run manually:
* Install node.js
* First do "npm install" (only needed first time)
* Then do "node server.js" or "nodemon server.js"
